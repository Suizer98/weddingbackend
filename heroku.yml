build:
  docker:
    web: Dockerfile

# Optional: You can include additional steps like testing or linting.
# test:
#   runs:
#     - python -m pytest

deploy:
  run:
    - black --check .
    - uvicorn wedding_app.main:app --host 0.0.0.0 --port 8000 --reload
